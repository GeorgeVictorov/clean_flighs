services:

  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: flights-image:latest

    env_file:
      - .env

    ports:
      - "5005:80"

    volumes:
      - ./src:/app/src
      - ./tests:/app/tests

